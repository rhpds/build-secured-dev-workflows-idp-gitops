global:
  auth:
    backend:
      enabled: true
  clusterRouterBase: apps.cluster-hzfgz.dynamic.redhatworkshops.io
  dynamic:
    includes:
      - dynamic-plugins.default.yaml
    plugins:
    # - disabled: false
    #   package: ./dynamic-plugins/dist/backstage-community-plugin-redhat-argocd
    # - disabled: false
    #   package: ./dynamic-plugins/dist/roadiehq-backstage-plugin-argo-cd-backend-dynamic
    #   pluginConfig:
    #     argocd:
    #       appLocatorMethods:
    #       - instances: []
    #         type: config
    # - disabled: false
    #   package: ./dynamic-plugins/dist/roadiehq-scaffolder-backend-argocd-dynamic
    #   pluginConfig:
    #     argocd:
    #       appLocatorMethods:
    #       - instances: []
    #         type: config
    # - disabled: false
    #   package: ./dynamic-plugins/dist/backstage-community-plugin-tekton
    #   pluginConfig:
    #     dynamicPlugins:
    #       frontend:
    #         backstage-community.plugin-tekton:
    #           mountPoints:
    #           - config:
    #               if:
    #                 allOf:
    #                 - isTektonCIAvailable
    #               layout:
    #                 gridColumn: 1 / -1
    #                 gridRowStart: 1
    #             importName: TektonCI
    #             mountPoint: entity.page.ci/cards
    # - disabled: false
    #   package: ./dynamic-plugins/dist/immobiliarelabs-backstage-plugin-gitlab
    # - disabled: false
    #   package: ./dynamic-plugins/dist/immobiliarelabs-backstage-plugin-gitlab-backend-dynamic
    # - disabled: false
    #   package: ./dynamic-plugins/dist/backstage-community-plugin-jenkins
    #   pluginConfig:
    #     dynamicPlugins:
    #       frontend:
    #         backstage-community.plugin-jenkins:
    #           mountPoints:
    #           - config:
    #               if:
    #                 allOf:
    #                 - isJenkinsAvailable
    #               layout:
    #                 gridColumn: 1 / -1
    #             importName: EntityJenkinsContent
    #             mountPoint: entity.page.ci/cards
    # - disabled: false
    #   package: oci://quay.io/redhat-tssc/backstage-plugins:1.7.0!tssc-plugins-backstage-community-plugin-multi-source-security-viewer-dynamic
    #   pluginConfig:
    #     dynamicPlugins:
    #       frontend:
    #         tssc-plugins.backstage-community-plugin-multi-source-security-viewer:
    #           entityTabs:
    #           - mountPoint: entity.page.ci
    #             path: /ci
    #             title: CI
    #           mountPoints:
    #           - config:
    #               if:
    #                 allOf:
    #                 - isMultiCIAvailable
    #               layout:
    #                 gridColumn: 1 / -1
    #                 gridRowStart: 2
    #             importName: EntityMultiCIPipelinesContent
    #             mountPoint: entity.page.ci/cards
    # - disabled: false
    #   package: ./dynamic-plugins/dist/backstage-plugin-scaffolder-backend-module-gitlab-dynamic
    # - disabled: false
    #   package: ./dynamic-plugins/dist/backstage-community-plugin-quay
    # - disabled: false
    #   package: ./dynamic-plugins/dist/backstage-plugin-kubernetes
    # - disabled: false
    #   package: ./dynamic-plugins/dist/backstage-plugin-kubernetes-backend-dynamic
    #   pluginConfig:
    #     kubernetes:
    #       clusterLocatorMethods:
    #       - clusters:
    #         - authProvider: serviceAccount
    #           customResources:
    #           - apiVersion: v1alpha1
    #             group: argoproj.io
    #             plural: rollouts
    #           - apiVersion: v1
    #             group: route.openshift.io
    #             plural: routes
    #           - apiVersion: v1
    #             group: tekton.dev
    #             plural: pipelineruns
    #           - apiVersion: v1
    #             group: tekton.dev
    #             plural: taskruns
    #           name: rhdh-cluster
    #           serviceAccountToken: ${K8S_SERVICEACCOUNT_TOKEN}
    #           skipTLSVerify: true
    #           url: https://kubernetes.default.svc
    #         type: config
    #       serviceLocatorMethod:
    #         type: multiTenant
    # - disabled: false
    #   package: ./dynamic-plugins/dist/backstage-community-plugin-topology
    # - disabled: false
    #   package: ./dynamic-plugins/dist/backstage-community-plugin-catalog-backend-module-keycloak-dynamic
    # - disabled: false
    #   package: ./dynamic-plugins/dist/backstage-plugin-techdocs
    # - disabled: false
    #   package: ./dynamic-plugins/dist/backstage-plugin-techdocs-backend-dynamic
    - disabled: false
      package: ./dynamic-plugins/dist/backstage-community-plugin-tech-radar

upstream:
  backstage:
    image:
      registry: quay.io
      repository: rhdh/rhdh-hub-rhel9
      tag: '1.8-164'
      pullSecrets:
      - redhat-gpte-devhub-pull-secret
    
    extraEnvVarsSecrets:
      - backstage-env-secret
    extraEnvVars:
      - name: BACKEND_URL
        value: https://backstage-backstage.apps.cluster-hzfgz.dynamic.redhatworkshops.io
      - name: ARGOCD_URL
        value: https://argocd-server-tssc-gitops.apps.cluster-hzfgz.dynamic.redhatworkshops.io
      - name: AUTH_OIDC_METADATA_URL
        value: https://sso.apps.cluster-hzfgz.dynamic.redhatworkshops.io/realms/sso/.well-known/openid-configuration
      - name: KEYCLOAK_LOGIN_REALM
        value: sso
      - name: KEYCLOAK_REALM
        value: sso
      - name: KEYCLOAK_BASE_URL
        value: https://sso.apps.cluster-hzfgz.dynamic.redhatworkshops.io
      - name: GITLAB_HOST
        value: gitlab-gitlab.apps.cluster-hzfgz.dynamic.redhatworkshops.io
      - name: QUAY_URL
        value: https://quay-hzfgz-1.apps.cluster-hzfgz.dynamic.redhatworkshops.io
      - name: NODE_TLS_REJECT_UNAUTHORIZED
        value: "0"

    appConfig:
      app:
        baseUrl: ${BACKEND_URL}
        title: Red Hat Developer Hub

      argocd:
        appLocatorMethods:
        - instances:
          - name: default
            url: ${ARGOCD_URL}
            username: ${ARGOCD_USER}
            password: ${ARGOCD_PASSWORD}
          type: config
        waitCycles: 25

      auth:
        environment: development
        providers:
          guest:
            dangerouslyAllowOutsideDevelopment: true
        # environment: production
        # providers:
        #   oidc:
        #     production:
        #       clientId: ${AUTH_OIDC_CLIENT_ID}
        #       clientSecret: ${AUTH_OIDC_CLIENT_SECRET}
        #       metadataUrl: ${AUTH_OIDC_METADATA_URL}
        #       prompt: auto
        #       signIn:
        #         resolvers:
        #         - resolver: preferredUsernameMatchingUserEntityName
        session:
          secret:
            $env: BACKEND_SECRET
      # signInPage: "oidc"
      backend:
        auth:
          keys:
          - secret: ${BACKEND_SECRET}
        baseUrl: ${BACKEND_URL}
        cors:
          origin: ${BACKEND_URL}
      catalog:
#         locations:
#         - target: https://gitlab-gitlab.apps.cluster-hzfgz.dynamic.redhatworkshops.io/rhdh/build-secured-dev-workflows-idp-configuration/blob/main/all-locations.yaml
#           type: url
#         rules:
#         - allow:
#           - Component
#           - System
#           - Group
#           - User
#           - Resource
#           - Location
#           - Template
#           - API
#         providers:
#           keycloakOrg:
#             default:
#               baseUrl: ${KEYCLOAK_BASE_URL}
#               clientId: ${KEYCLOAK_CLIENT_ID}
#               clientSecret: ${KEYCLOAK_CLIENT_SECRET}
#               loginRealm: ${KEYCLOAK_LOGIN_REALM}
#               realm: ${KEYCLOAK_REALM}
#               schedule:
#                 frequency:
#                   minutes: 2
#                 initialDelay:
#                   seconds: 15
#                 timeout:
#                   minutes: 2
#

    # integrations:
    #   gitlab:
    #     - host: ${GITLAB_HOST}
    #       baseUrl: https://${GITLAB_HOST}
    #       apiBaseUrl: https://${GITLAB_HOST}/api/v4
    #       token: ${GITLAB_TOKEN}
            
      permission:
        enabled: false
      proxy:
        endpoints:
          # '/quay/api':
          #   changeOrigin: true
          #   headers:
          #     Authorization: Bearer ${QUAY_API_TOKEN}
          #     X-Requested-With: XMLHttpRequest
          #   secure: true
          #   target: ${QUAY_URL}
          '/developer-hub':
            target: https://${GITLAB_HOST}/
            pathRewrite:
              '^/api/proxy/developer-hub/tech-radar': /gitops/build_secured_dev_workflows_idp_gitops/-/raw/main/config/tech-radar.json
            changeOrigin: true
            secure: false

      quay:
        uiUrl: ${QUAY_URL}
      techdocs:
        builder: local
        generator:
          runIn: local
        publisher:
          type: local

      enabled:
        kubernetes: true
        techdocs: true
        argocd: true
        sonarqube: false
        keycloak: true
        ocm: true
        github: false
        githubOrg: false
        gitlab: true
        jenkins: false
        permission: false

  postgresql:
    auth:
      username: bn_backstage
      postgresUser: postgres
      database: postgres
      existingSecret: backstage-postgresql
      secretKeys:
        adminPasswordKey: postgres-password
        userPasswordKey: password

route:
  enabled: true
  host: backstage-backstage.apps.cluster-hzfgz.dynamic.redhatworkshops.io
  tls:
    enabled: true
    termination: "edge"
